-- ========== TELEPORT & UTILITY GUI (MOBILE FRIENDLY) ==========
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

if playerGui:FindFirstChild("TeleportGUI") then
    playerGui.TeleportGUI:Destroy()
end

-- Daftar lokasi
local locations = {
    {name = "Position Awal", pos = Vector3.new(214, 13, -2936)},
    {name = "Heist", pos = Vector3.new(252, -48, -1679)},
    {name = "Bank", pos = Vector3.new(677, -3, -1198)},
    {name = "Gas Station", pos = Vector3.new(736, -32, -486)},
    {name = "Pintu Bank", pos = Vector3.new(690, -32, -1224)},
    {name = "Pintu Heist", pos = Vector3.new(293, 7, -1710)},
    {name = "Mansion", pos = Vector3.new(316, 0, -2459)},
}

-- ================= STATE =================
local noclipEnabled = false
local infJumpEnabled = false
local wsLoopEnabled = false
local wsValue = 30
local instantPPEnabled = false

local noclipConn = nil
local infJumpConn
local wsConn
local instantPPConn
local originalHoldDurations = {}

-- ================= FUNGSI TELEPORT =================
local function getChar()
    local character = player.Character or player.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart")
    return character, hrp
end

local function teleportTo(pos)
    local _, hrp = getChar()
    hrp.CFrame = CFrame.new(pos)
end

-- ================= FUNGSI NOCLIP (FIXED) =================
local function setNoclip(state)
    noclipEnabled = state

    -- PENTING: selalu putuskan koneksi lama dulu, apapun statenya,
    -- supaya tidak ada koneksi "hantu" yang terus memaksa CanCollide=false
    if noclipConn then
        noclipConn:Disconnect()
        noclipConn = nil
    end

    if state then
        noclipConn = RunService.Stepped:Connect(function()
            -- Cek ulang tiap frame apakah noclip masih aktif,
            -- kalau sudah dimatikan di tengah jalan, hentikan diri sendiri
            if not noclipEnabled then
                if noclipConn then
                    noclipConn:Disconnect()
                    noclipConn = nil
                end
                return
            end
            local character = player.Character
            if character then
                for _, part in pairs(character:GetDescendants()) do
                    if part:IsA("BasePart") and part.CanCollide then
                        part.CanCollide = false
                    end
                end
            end
        end)
    else
        local character = player.Character
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
end

-- ================= FUNGSI INFINITE JUMP =================
local function setInfJump(state)
    infJumpEnabled = state
    if infJumpConn then
        infJumpConn:Disconnect()
        infJumpConn = nil
    end
    if state then
        infJumpConn = UserInputService.JumpRequest:Connect(function()
            local character = player.Character
            if character then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end
        end)
    end
end

-- ================= FUNGSI WALKSPEED LOOP =================
local function setWSLoop(state, value)
    wsLoopEnabled = state
    wsValue = value or wsValue
    if wsConn then
        wsConn:Disconnect()
        wsConn = nil
    end
    if state then
        wsConn = RunService.Heartbeat:Connect(function()
            if not wsLoopEnabled then
                if wsConn then
                    wsConn:Disconnect()
                    wsConn = nil
                end
                return
            end
            local character = player.Character
            if character then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid.WalkSpeed = wsValue
                end
            end
        end)
    else
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = 16
            end
        end
    end
end

-- ================= FUNGSI INSTANT PROXIMITY PROMPT =================
local function collectPrompts()
    local prompts = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            table.insert(prompts, obj)
        end
    end
    return prompts
end

local function setInstantPP(state)
    instantPPEnabled = state
    if instantPPConn then
        instantPPConn:Disconnect()
        instantPPConn = nil
    end
    if state then
        for _, prompt in ipairs(collectPrompts()) do
            if originalHoldDurations[prompt] == nil then
                originalHoldDurations[prompt] = prompt.HoldDuration
            end
            prompt.HoldDuration = 0
        end
        instantPPConn = workspace.DescendantAdded:Connect(function(obj)
            if obj:IsA("ProximityPrompt") then
                originalHoldDurations[obj] = obj.HoldDuration
                obj.HoldDuration = 0
            end
        end)
    else
        for prompt, dur in pairs(originalHoldDurations) do
            if prompt and prompt.Parent then
                prompt.HoldDuration = dur
            end
        end
        originalHoldDurations = {}
    end
end

-- ========== BUAT GUI (MOBILE) ==========
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "TeleportGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local ROW_HEIGHT = 26
local ROW_GAP = 4
local TITLE_HEIGHT = 28
local TOTAL_ROWS = #locations + 4

local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 150, 0, TITLE_HEIGHT + (TOTAL_ROWS * (ROW_HEIGHT + ROW_GAP)) + 8)
mainFrame.Position = UDim2.new(0.5, -75, 0.5, -180)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 6)
corner.Parent = mainFrame

local titleBar = Instance.new("Frame")
titleBar.Name = "TitleBar"
titleBar.Size = UDim2.new(1, 0, 0, TITLE_HEIGHT)
titleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleCorner = Instance.new("UICorner")
titleCorner.CornerRadius = UDim.new(0, 6)
titleCorner.Parent = titleBar

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -58, 1, 0)
titleLabel.Position = UDim2.new(0, 6, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Menu"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 12
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = titleBar

local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 22, 0, 22)
minimizeBtn.Position = UDim2.new(1, -50, 0, 3)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
minimizeBtn.Text = "-"
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextSize = 16
minimizeBtn.Parent = titleBar

local minimizeCorner = Instance.new("UICorner")
minimizeCorner.CornerRadius = UDim.new(0, 5)
minimizeCorner.Parent = minimizeBtn

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 22, 0, 22)
closeBtn.Position = UDim2.new(1, -24, 0, 3)
closeBtn.BackgroundColor3 = Color3.fromRGB(170, 40, 40)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 12
closeBtn.Parent = titleBar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 5)
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

local buttonContainer = Instance.new("Frame")
buttonContainer.Name = "ButtonContainer"
buttonContainer.Size = UDim2.new(1, -12, 1, -(TITLE_HEIGHT + 8))
buttonContainer.Position = UDim2.new(0, 6, 0, TITLE_HEIGHT + 4)
buttonContainer.BackgroundTransparency = 1
buttonContainer.Parent = mainFrame

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, ROW_GAP)
listLayout.Parent = buttonContainer

local function createLocationButton(text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 11
    btn.Parent = buttonContainer

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = btn

    btn.MouseButton1Click:Connect(callback)

    btn.MouseEnter:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(65, 65, 75)
    end)
    btn.MouseLeave:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    end)

    return btn
end

for _, loc in ipairs(locations) do
    createLocationButton(loc.name, function()
        teleportTo(loc.pos)
    end)
end

local function createToggleButton(text, onToggle)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    btn.Text = text .. ": OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 11
    btn.Parent = buttonContainer

    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = btn

    local state = false
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = text .. ": " .. (state and "ON" or "OFF")
        btn.BackgroundColor3 = state and Color3.fromRGB(40, 110, 60) or Color3.fromRGB(45, 45, 50)
        onToggle(state)
    end)

    return btn
end

createToggleButton("Noclip", function(state)
    setNoclip(state)
end)

createToggleButton("Inf Jump", function(state)
    setInfJump(state)
end)

createToggleButton("Instant PP", function(state)
    setInstantPP(state)
end)

local wsFrame = Instance.new("Frame")
wsFrame.Size = UDim2.new(1, 0, 0, ROW_HEIGHT)
wsFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
wsFrame.Parent = buttonContainer

local wsCorner = Instance.new("UICorner")
wsCorner.CornerRadius = UDim.new(0, 5)
wsCorner.Parent = wsFrame

local wsToggleBtn = Instance.new("TextButton")
wsToggleBtn.Size = UDim2.new(0.58, -2, 1, 0)
wsToggleBtn.Position = UDim2.new(0, 0, 0, 0)
wsToggleBtn.BackgroundTransparency = 1
wsToggleBtn.Text = "WS: OFF"
wsToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
wsToggleBtn.Font = Enum.Font.Gotham
wsToggleBtn.TextSize = 11
wsToggleBtn.Parent = wsFrame

local wsInput = Instance.new("TextBox")
wsInput.Size = UDim2.new(0.42, -6, 1, -4)
wsInput.Position = UDim2.new(0.58, 4, 0, 2)
wsInput.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
wsInput.Text = "30"
wsInput.PlaceholderText = "30"
wsInput.TextColor3 = Color3.fromRGB(255, 255, 255)
wsInput.Font = Enum.Font.Gotham
wsInput.TextSize = 11
wsInput.ClearTextOnFocus = false
wsInput.Parent = wsFrame

local wsInputCorner = Instance.new("UICorner")
wsInputCorner.CornerRadius = UDim.new(0, 5)
wsInputCorner.Parent = wsInput

wsInput.FocusLost:Connect(function()
    local num = tonumber(wsInput.Text)
    if num then
        wsValue = num
        if wsLoopEnabled then
            setWSLoop(true, wsValue)
        end
    else
        wsInput.Text = tostring(wsValue)
    end
end)

wsToggleBtn.MouseButton1Click:Connect(function()
    wsLoopEnabled = not wsLoopEnabled
    wsToggleBtn.Text = "WS: " .. (wsLoopEnabled and "ON" or "OFF")
    wsFrame.BackgroundColor3 = wsLoopEnabled and Color3.fromRGB(40, 110, 60) or Color3.fromRGB(45, 45, 50)
    setWSLoop(wsLoopEnabled, tonumber(wsInput.Text) or 30)
end)

local minimized = false
local expandedSize = mainFrame.Size

minimizeBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    if minimized then
        buttonContainer.Visible = false
        mainFrame.Size = UDim2.new(0, 150, 0, TITLE_HEIGHT + 8)
        minimizeBtn.Text = "+"
    else
        buttonContainer.Visible = true
        mainFrame.Size = expandedSize
        minimizeBtn.Text = "-"
    end
end)

local dragging = false
local dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    mainFrame.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y
    )
end

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

titleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

player.CharacterAdded:Connect(function(character)
    task.wait(1)
    if noclipEnabled then
        setNoclip(true)
    end
    if wsLoopEnabled then
        setWSLoop(true, wsValue)
    end
end)
